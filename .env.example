# Public values may be exposed to browser code.
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Never expose this value to browser code. Trusted server environments only.
SUPABASE_SERVICE_ROLE_KEY=

# Real values must never be committed. No live Supabase connection is required yet.
