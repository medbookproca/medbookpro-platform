# Browser-safe values. These may be exposed to browser bundles.
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Do not add service-role keys, database passwords, JWT secrets, or SMTP credentials here.
# SUPABASE_SERVICE_ROLE_KEY is intentionally not required by this application.
# Real values must never be committed.
