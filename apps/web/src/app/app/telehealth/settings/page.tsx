import { Card } from '@medbookpro/ui';
import { getActiveOrganizationContext } from '@/lib/organization-context';
import { requireAuthenticatedUser } from '@/lib/supabase/auth-helpers';
import { createClient } from '@/lib/supabase/server';

export default async function TelehealthSettingsPage() { const user = await requireAuthenticatedUser('/app/telehealth/settings'); const context = await getActiveOrganizationContext(user.id); if (!context) return null; const supabase = await createClient(); const { data, error } = await supabase.from('telehealth_provider_settings').select('provider,display_name,enabled').eq('organization_id', context.organizationId).order('provider'); if (error) throw error; const providers = ['zoom','google_meet','microsoft_teams','daily','twilio','custom_provider']; return <main className="min-h-screen bg-slate-50 px-6 py-12"><div className="mx-auto max-w-4xl"><h1 className="text-4xl font-semibold">Provider configuration</h1><p className="mt-3 text-slate-600">Approved provider placeholders only. No provider credentials or connections are configured.</p><div className="mt-8 grid gap-4 sm:grid-cols-2">{providers.map((provider) => { const setting = data?.find((item) => item.provider === provider); return <Card key={provider}><p className="font-semibold">{setting?.display_name ?? provider}</p><p className="mt-2 text-sm text-slate-600">{setting?.enabled ? 'Enabled placeholder' : 'Not configured'}</p></Card>; })}</div></div></main>; }
