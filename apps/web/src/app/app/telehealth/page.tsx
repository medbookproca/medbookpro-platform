import Link from 'next/link';
import { Card } from '@medbookpro/ui';
import { getActiveOrganizationContext } from '@/lib/organization-context';
import { requireAuthenticatedUser } from '@/lib/supabase/auth-helpers';
import { createClient } from '@/lib/supabase/server';
import { createTelehealthSessionAction } from './actions';

export default async function TelehealthPage() { const user = await requireAuthenticatedUser('/app/telehealth'); const context = await getActiveOrganizationContext(user.id); if (!context) return null; const supabase = await createClient(); const { data, error } = await supabase.from('telehealth_sessions').select('id,appointment_id,patient_id,practitioner_id,scheduled_start,scheduled_end,status,provider_placeholder').eq('organization_id', context.organizationId).order('scheduled_start'); if (error) throw error; return <main className="min-h-screen bg-slate-50 px-6 py-12 text-slate-950"><div className="mx-auto max-w-6xl"><div className="flex flex-wrap justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-wide text-blue-700">Telehealth</p><h1 className="mt-2 text-4xl font-semibold">Session dashboard</h1><p className="mt-3 text-slate-600">Placeholder virtual-care metadata only. No video provider is connected.</p></div><nav className="flex gap-3 text-sm"><Link className="text-blue-700 underline" href="/app/telehealth/upcoming">Upcoming</Link><Link className="text-blue-700 underline" href="/app/telehealth/sessions">Sessions</Link><Link className="text-blue-700 underline" href="/app/telehealth/waiting-room">Waiting room</Link><Link className="text-blue-700 underline" href="/app/telehealth/settings">Provider settings</Link></nav></div><Card className="mt-8"><h2 className="text-xl font-semibold">Create session metadata</h2><form action={createTelehealthSessionAction} className="mt-4 grid gap-4 md:grid-cols-2"><input required name="appointmentId" placeholder="Appointment UUID" className="rounded-lg border border-slate-300 px-3 py-2" /><input required name="patientId" placeholder="Patient UUID" className="rounded-lg border border-slate-300 px-3 py-2" /><input required name="practitionerId" placeholder="Practitioner UUID" className="rounded-lg border border-slate-300 px-3 py-2" /><input name="locationId" placeholder="Location UUID (optional)" className="rounded-lg border border-slate-300 px-3 py-2" /><input required type="datetime-local" name="scheduledStart" className="rounded-lg border border-slate-300 px-3 py-2" /><input required type="datetime-local" name="scheduledEnd" className="rounded-lg border border-slate-300 px-3 py-2" /><select name="provider" defaultValue="custom_provider" className="rounded-lg border border-slate-300 px-3 py-2"><option value="custom_provider">Custom provider placeholder</option><option value="zoom">Zoom placeholder</option><option value="google_meet">Google Meet placeholder</option><option value="microsoft_teams">Microsoft Teams placeholder</option><option value="daily">Daily placeholder</option><option value="twilio">Twilio placeholder</option></select><button className="rounded-lg bg-blue-600 px-4 py-2.5 font-medium text-white" type="submit">Create session metadata</button></form></Card><div className="mt-8 space-y-4">{data?.map((session) => <Card key={session.id}><div className="flex flex-wrap justify-between gap-4"><div><h2 className="font-semibold">{session.scheduled_start}</h2><p className="text-sm text-slate-600">{session.status} · {session.provider_placeholder ?? 'No provider'}</p></div><Link className="text-sm font-medium text-blue-700 underline" href={`/app/telehealth/sessions?sessionId=${session.id}`}>Open session</Link></div></Card>)}</div></div></main>; }
